# Copy to .env for local development (never commit .env)

DATABASE_URL=postgresql://localhost:5432/constituent_tracker
SESSION_SECRET=change-me-to-a-long-random-string
APP_PUBLIC_URL=http://127.0.0.1:5173
PORT=8080
NODE_ENV=development
PASSWORD_HIBP_DISABLED=1

# Optional — SAML
# SAML_IDP_CERT_FINGERPRINT_SHA256=
# SAML_IDP_METADATA_HOST_ALLOWLIST=login.microsoftonline.com

# Optional — email
# RESEND_API_KEY=
# EMAIL_FROM=
